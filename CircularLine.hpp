#include <iostream>
#include <vector>
#include "stdlib.h"


using namespace std;

class CircularLine{

    public:
        int longestTravelled(vector<int> t){
            int shortestDisance; // Returning an int value for method
            vector<int> distances; // Distances corresponding to each vector element

            for (int i = 0; i < t.size(); i++){
                
            }
        }
}
#include <iostream>
#include <string.h>
#include <stdlib.h>

using namespace std;

class StreetParking{
    public:
        int freeParks(string street){

            int counter = 0;
            int totalSpaceAvaiable = street.length()*5;

            // for (int i = 0; i < street.length(); i++){
            //     if (street[i] == -)
            // }

            cout << totalSpaceAvaiable << endl;
        }
};

